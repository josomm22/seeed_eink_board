[project]
name = "seeed-eink-board"
version = "0.1.0"
description = "Build tooling for the EE02 e-ink display firmware (PlatformIO via uv)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "platformio>=6.1.18",
]
